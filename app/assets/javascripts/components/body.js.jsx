/** @jsx React.DOM */
// var React = require('react');

window.Body = React.createClass({
    displayName: 'Body',
    render: function () {
        return (
            <div>Body</div>
        );
    }
});

module.exports = Body;