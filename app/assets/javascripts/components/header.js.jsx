/** @jsx React.DOM */
// var React = require('react');

window.Header = React.createClass({
    displayName: 'Header',
    render: function () {
        return (
            <div className="well">Header</div>
        );
    }
});

module.exports = Header;